--- a/dist/core/session-manager.js
+++ b/dist/core/session-manager.js
@@ -7,6 +7,7 @@
 import { StringDecoder } from "string_decoder";
 import { APP_NAME, getAgentDir as getDefaultAgentDir, getSessionsDir } from "../config.js";
 import { normalizePath, resolvePath } from "../utils/paths.js";
+import { loadBoundedSessionEntriesSync, sessionFileNeedsTrailingNewlineSync } from "./bounded-session-reader.js";
 import { createBranchSummaryMessage, createCompactionSummaryMessage, createCustomMessage, } from "./messages.js";
 export const CURRENT_SESSION_VERSION = 3;
 function createSessionId() {
@@ -252,7 +253,6 @@
     }
     return sessionDir;
 }
-const SESSION_READ_BUFFER_SIZE = 1024 * 1024;
 const SESSION_HEADER_READ_BUFFER_SIZE = 4096;
 /** Bound synchronous header discovery while allowing large cwd and custom metadata fields. */
 const MAX_SESSION_HEADER_SCAN_BYTES = 1024 * 1024;
@@ -278,36 +278,7 @@
     const resolvedFilePath = normalizePath(filePath);
     if (!existsSync(resolvedFilePath))
         return [];
-    const entries = [];
-    let pending = "";
-    const fd = openSync(resolvedFilePath, "r");
-    try {
-        const decoder = new StringDecoder("utf8");
-        const buffer = Buffer.allocUnsafe(SESSION_READ_BUFFER_SIZE);
-        while (true) {
-            const bytesRead = readSync(fd, buffer, 0, buffer.length, null);
-            if (bytesRead === 0)
-                break;
-            pending += decoder.write(buffer.subarray(0, bytesRead));
-            let lineStart = 0;
-            let newlineIndex = pending.indexOf("\n", lineStart);
-            while (newlineIndex !== -1) {
-                const entry = parseSessionEntryLine(pending.slice(lineStart, newlineIndex));
-                if (entry)
-                    entries.push(entry);
-                lineStart = newlineIndex + 1;
-                newlineIndex = pending.indexOf("\n", lineStart);
-            }
-            pending = pending.slice(lineStart);
-        }
-        pending += decoder.end();
-        const finalEntry = parseSessionEntryLine(pending);
-        if (finalEntry)
-            entries.push(finalEntry);
-    }
-    finally {
-        closeSync(fd);
-    }
+    const entries = loadBoundedSessionEntriesSync(resolvedFilePath);
     // Validate session header before repairing the file.
     if (entries.length === 0)
         return entries;
@@ -315,7 +286,7 @@
     if (header.type !== "session" || typeof header.id !== "string") {
         return [];
     }
-    if (pending)
+    if (sessionFileNeedsTrailingNewlineSync(resolvedFilePath))
         appendFileSync(resolvedFilePath, "\n");
     return entries;
 }
