--- a/node_modules/@earendil-works/pi-tui/dist/tui.js
+++ b/node_modules/@earendil-works/pi-tui/dist/tui.js
@@ -70,6 +70,23 @@
  * TUI - Main class for managing terminal UI with differential rendering
  */
 const SEGMENT_RESET = "\x1b[0m\x1b]8;;\x07";
+const DEFAULT_CANVAS_BACKGROUND = "#080B1A";
+const configuredCanvasBackground = process.env.PI_TUI_CANVAS_BG ?? DEFAULT_CANVAS_BACKGROUND;
+const canvasMatch = /^#([\da-f]{2})([\da-f]{2})([\da-f]{2})$/iu.exec(configuredCanvasBackground);
+const CANVAS_BACKGROUND_SGR = canvasMatch
+    ? `\x1b[48;2;${Number.parseInt(canvasMatch[1], 16)};${Number.parseInt(canvasMatch[2], 16)};${Number.parseInt(canvasMatch[3], 16)}m`
+    : undefined;
+const BACKGROUND_RESET = /\x1b\[(?:0|49)m/gu;
+/** Apply Pi's own canvas background without changing the terminal or multiplexer palette. */
+export function applyTuiCanvasBackground(line, width) {
+    if (!CANVAS_BACKGROUND_SGR || isImageLine(line))
+        return line;
+    const boundedWidth = Math.max(0, Math.floor(width));
+    const content = visibleWidth(line) <= boundedWidth ? line : sliceByColumn(line, 0, boundedWidth, true);
+    const colored = content.replace(BACKGROUND_RESET, (reset) => `${reset}${CANVAS_BACKGROUND_SGR}`);
+    const padding = " ".repeat(Math.max(0, boundedWidth - visibleWidth(content)));
+    return `${CANVAS_BACKGROUND_SGR}${colored}${padding}\x1b[0m`;
+}
 /** Composite overlay content into a terminal line at a fixed column. */
 export function compositeTuiLine(baseLine, overlayLine, startCol, overlayWidth, totalWidth) {
     if (isImageLine(baseLine))
--- a/node_modules/@earendil-works/pi-tui/dist/tui-alt-screen.js
+++ b/node_modules/@earendil-works/pi-tui/dist/tui-alt-screen.js
@@ -9,2 +9,3 @@
+import { applyTuiCanvasBackground } from "./tui.js";
 const ENTER_ALT_SCREEN = "\x1b[?1049h";
 const EXIT_ALT_SCREEN = "\x1b[?1049l";
@@ -852,11 +852,15 @@
         screen = this.applySelection(screen, nextLayout);
         screen = this.compositeFlashes(screen, width, height);
         const cursorPos = this.extractCursorPosition(screen, height);
-        screen = this.applyLineResets(screen).map((line) => {
+        while (screen.length < height)
+            screen.push("");
+        screen = this.applyLineResets(screen)
+            .map((line) => {
             if (isImageLine(line) || visibleWidth(line) <= width)
                 return line;
             return sliceByColumn(line, 0, width, true);
-        });
+        })
+            .map((line) => applyTuiCanvasBackground(line, width));
         const fullRedraw = this.previousScreen.length === 0 || this.previousScreenWidth !== width || this.previousScreenHeight !== height;
         const imagesNeedRedraw = screen.some((line, row) => line !== this.previousScreen[row] && (isImageLine(line) || isImageLine(this.previousScreen[row] ?? "")));
         const redrawImages = fullRedraw || imagesNeedRedraw;
--- a/node_modules/@earendil-works/pi-tui/dist/tui-main-screen.js
+++ b/node_modules/@earendil-works/pi-tui/dist/tui-main-screen.js
@@ -3,3 +3,3 @@
 import { deleteKittyImage, isImageLine } from "./terminal-image.js";
-import { TuiBase } from "./tui.js";
+import { applyTuiCanvasBackground, TuiBase } from "./tui.js";
 import { visibleWidth } from "./utils.js";
@@ -174,7 +174,7 @@
         }
         // Extract cursor position before applying line resets (marker must be found first)
         const cursorPos = this.extractCursorPosition(newLines, height);
-        newLines = this.applyLineResets(newLines);
+        newLines = this.applyLineResets(newLines).map((line) => applyTuiCanvasBackground(line, width));
         // Helper to clear scrollback and viewport and render all new lines
         const fullRender = (clear) => {
             this.fullRedrawCount += 1;
